/**
 * Public API for {{projectName}}.
 *
 * Re-export the library's public surface from this file. Anything not exported
 * here is treated as internal and may change without a semver bump.
 */
export const VERSION = "0.0.0";
